//Definir dos variables constantes
const primerNumero = 30;
const segundoNumero = 70;
//Llamar a la función, sumar, pasar los valores y capturar el resultado
var ver = sumar(primerNumero,segundoNumero);
console.log(ver);
//Llamar a la función, restar, pasar los valores y capturar el resultado
/* Llamar a la función, comparar los valores, pasar los valores, fijarse
que el primer valor sea mayor y capturar el resultado */
//Imprimir los resultados en consola
//Imprimir el tipo de resltado en consola

const suma = sumar(1,2);
console.log(suma);
function sumar(num1,num2)
{
    let operacion = num1 + num2
    return operacion;
}